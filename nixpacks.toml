[variables]
NIXPACKS_JDK_VERSION = "21"
JAVA_VERSION = "21"

[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["java -version", "mvn -version", "mvn -DskipTests package"]

[start]
cmd = "java -jar target/rinconcito-marino-0.0.1-SNAPSHOT.jar"
